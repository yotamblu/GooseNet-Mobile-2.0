import { Dimensions } from 'react-native';

const { width, height } = Dimensions.get('window');

export const GRID_SIZE = 40; // Grid square size in pixels
export const GRID_COLOR = 'rgba(42, 95, 191, 0.1)'; // Grid Line Blue #2A5FBF at 10% opacity

export { width, height };

